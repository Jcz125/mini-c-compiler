struct S2 {
    int a ;
    int b ;
};

int fct(struct S2 * ab, int c) {
    while (c > 0)
        while (ab->a > 0)  {
            return 0 ;
        }
    return 1;
}

int main() {
    struct S2 * d = malloc(sizeof(struct S2));
    d->a = 10;
    d->b = 5;
    return fct(d, 5) ;
}