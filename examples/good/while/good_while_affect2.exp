struct S2 {
    int a ;
    int b ;
};

int fct(int c, int d) {
    struct S2 * ab ; //= malloc(sizeof(struct S2));
    while (c > 0)
        c = c-1 ;
    return 0;
}

int main() {
    return fct(3, 5) ;
}